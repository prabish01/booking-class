module.exports = (plugin) => {
  console.log("🔧 Loading users-permissions extension...");

  // Use Strapi's default functionality - no custom overrides
  console.log("✅ Using Strapi's default users-permissions APIs");

  return plugin;
};
