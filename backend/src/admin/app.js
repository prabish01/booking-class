export default {
  config: {
    // Custom configuration for admin panel
    locales: ["en"],
  },
  bootstrap(app) {
    // Custom bootstrap logic
    console.log("Admin panel bootstrapped");
  },
};
